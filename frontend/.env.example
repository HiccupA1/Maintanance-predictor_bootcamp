# Base URL of the Work Order Management backend (FastAPI).
# Endpoints are versioned under /v1 (e.g. GET {VITE_API_BASE_URL}/v1/work-orders).
# Request this value from the user/orchestrator for non-local environments.
VITE_API_BASE_URL=http://localhost:8000
